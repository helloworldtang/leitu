# 基线记录

## 2026-09-21 · 预演（非独立采样）

- 执行者：已了解仓库的助手（非全新 agent）——按任务卡约束完成，作为流程贯通与参考解校准，不作为统计基线。
- 结果：完成 ✅；一次绿灯 ✅（`mvn test` 首跑 7/7）；机器侧构建 + 测试约 2.6 秒（非人工时长口径）。
- 歧义点（已转化为改进工单，本次随交付同步修补）：
  1. 依赖组合（Web / JDBC / 测试模块）只能从参考项目 pom 推出 → 已补 README「在业务项目中接入」段；
  2. 控制器参数名需要 `-parameters` 编译标志 → 已在 README 接入段写明；
  3. Boot 4 测试基建需要 JUnit 6 → 由 Boot BOM 管理，README 接入段注明「标准 Spring 组合」；
  4. 入口 Filter 命名与 Boot 内置 Bean 的冲突风险 → 金样本内注释说明；
  5. leitu-rules 下游用法未说明「双方包都要导入」 → 已修 LeituRules javadoc（本目录参考解内含可运行示范）。
