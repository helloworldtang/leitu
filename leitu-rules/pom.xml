<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>cn.youhuale</groupId>
    <artifactId>leitu-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>leitu-rules</artifactId>
  <name>leitu-rules</name>
  <description>累土的架构边界规则集（main 代码，可发布）：本仓库全 reactor 扫描；下游项目依赖后用同一套规则守自己的架构。</description>

  <dependencies>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit</artifactId>
    </dependency>

    <!-- repo 级扫描的观测对象（test 依赖=金丝雀监视范围，非复用） -->
    <dependency>
      <groupId>cn.youhuale</groupId>
      <artifactId>leitu-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>cn.youhuale</groupId>
      <artifactId>leitu-example-allow-or-not</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>cn.youhuale</groupId>
      <artifactId>leitu-example-who-is-operating</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>cn.youhuale</groupId>
      <artifactId>leitu-capability-access</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>cn.youhuale</groupId>
      <artifactId>leitu-example-access</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
